{
  "name": "html-to-apk",
  "version": "1.0.0",
  "description": "Convert HTML to APK using Capacitor",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Building web assets...'",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
