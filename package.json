{
  "name": "purtv-donghua",
  "version": "1.0.0",
  "description": "PurTV Donghua - Watch Chinese Animation",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static web app - no build needed'",
    "cap:sync": "npx cap sync",
    "cap:build:android": "npx cap build android --keystorepath purtv.keystore --keystorepass $KEYSTORE_PASS --keystorealias purtv --keystorealiaspass $KEYSTORE_PASS"
  },
  "keywords": ["purtv", "donghua", "anime", "capacitor"],
  "author": "PurTV Team",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/network": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
